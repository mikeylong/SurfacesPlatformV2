# P0 Demo Viewer

Build the static P0 proof viewer from the current proof artifacts:

```bash
npm run demo:p0
```

Open `demo/p0/index.html` in a browser. The file is self-contained and embeds the current P0 evidence, adapter diagnostics, governed catalog, source fixture, and expectations manifest.
