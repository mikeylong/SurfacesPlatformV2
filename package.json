{
  "name": "surfaces-platform-v2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "interfacectl": "./bin/interfacectl"
  },
  "scripts": {
    "materialize:p0": "node scripts/materialize-p0.mjs",
    "proof:p0": "node bin/interfacectl.js surfaces proof --fixture fixtures/p0 --out artifacts/p0",
    "check:p0": "npm run materialize:p0 && npm run proof:p0 && npm test",
    "check:p0:ci": "npm run check:p0 && git diff --exit-code -- schemas fixtures artifacts",
    "test": "node --test"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
